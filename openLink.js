// var time = Time of zoom meeting
var today = new Date();
var currentMinute = today.getMinutes();
var currentHour = today.getHours();

document.querySelector("")

while (currentHour + 1 == time.hour && currentMinute + 5 == time.minute) {
    window.open(zoomLink);
}